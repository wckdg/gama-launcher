name: Build All Platforms

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows-installer:
    name: Build Windows Installer
    runs-on: windows-latest
    
    steps:
      - name: Checkout code with LFS
        uses: actions/checkout@v4
        with:
          lfs: true
      
      - name: Checkout LFS objects
        run: git lfs pull
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: pip install pyinstaller customtkinter pillow psutil GPUtil requests urllib3 certifi
      
      - name: Install Inno Setup
        run: choco install innosetup -y
        shell: powershell
      
      - name: Build Installer
        run: python build_installer.py
        env:
          PYTHONIOENCODING: utf-8
      
      - name: Upload Windows Installer
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer
          path: dist/GamaLauncher-Setup-v*.exe

  build-windows-portable:
    name: Build Windows Portable
    runs-on: windows-latest
    
    steps:
      - name: Checkout code with LFS
        uses: actions/checkout@v4
        with:
          lfs: true
      
      - name: Checkout LFS objects
        run: git lfs pull
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: pip install pyinstaller customtkinter pillow psutil GPUtil requests urllib3 certifi
      
      - name: Build Portable ZIP
        run: python build_portable.py
        env:
          PYTHONIOENCODING: utf-8
      
      - name: Upload Windows Portable
        uses: actions/upload-artifact@v4
        with:
          name: windows-portable
          path: dist/GamaLauncher-Portable-v*.zip

  build-macos:
    name: Build macOS
    runs-on: macos-latest
    
    steps:
      - name: Checkout code with LFS
        uses: actions/checkout@v4
        with:
          lfs: true
      
      - name: Checkout LFS objects
        run: git lfs pull
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          pip install pyinstaller customtkinter pillow psutil GPUtil requests urllib3 certifi
          brew install create-dmg
      
      - name: Build macOS App
        run: python build_macos.py
      
      - name: Sign app bundle (ad-hoc)
        run: |
          codesign --force --deep --sign - dist/GamaLauncher.app
          codesign --verify --verbose dist/GamaLauncher.app
        continue-on-error: true
      
      - name: Upload macOS DMG
        uses: actions/upload-artifact@v4
        with:
          name: macos-dmg
          path: dist/GamaLauncher-v*-macOS.dmg

  create-release:
    name: Create Release
    needs: [build-windows-installer, build-windows-portable, build-macos]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/download-artifact@v4
        with:
          name: windows-installer
          path: release/
      
      - uses: actions/download-artifact@v4
        with:
          name: windows-portable
          path: release/
      
      - uses: actions/download-artifact@v4
        with:
          name: macos-dmg
          path: release/
      
      - name: Get version
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: Gama Launcher v${{ steps.version.outputs.VERSION }}
          body: |
            ## 🎮 Gama Launcher v${{ steps.version.outputs.VERSION }}
            
            ### 📥 Downloads
            
            **Windows:**
            - **Installer** (.exe) - Recommended for most users
              - Installs to Program Files
              - Adds Start Menu shortcuts
              - Automatic updates
            - **Portable** (.zip) - No installation required
              - Extract and run anywhere
              - Perfect for USB drives
              - All files in one folder
            
            **macOS:**
            - **DMG Installer** (.dmg) - For Apple Silicon (M1/M2/M3) Macs
              - Drag to Applications
              - **Important:** Right-click → Open on first launch
            
            ---
            
            ### 🎯 Features
            - ✅ Quality presets: Very Low → Ultra
            - ✅ Optional shader support (Complementary)
            - ✅ Pre-downloaded game assets (~400MB, instant setup)
            - ✅ All mods included (83-93 mods depending on tier)
            - ✅ Automatic Java 17 download
            - ✅ Automatic Minecraft + Fabric installation
            - ✅ Smart system recommendations
            - ✅ Server auto-configured in multiplayer list
            
            ### 🖥️ System Requirements
            - **Minimum:** 4GB RAM, integrated graphics
            - **Recommended:** 8GB+ RAM, dedicated GPU
            - **For Shaders:** 12GB+ RAM, GTX 1060 / RX 580 or better
            
            ### 🌐 Server
            - **Name:** GAMA KLUB
            - **IP:** `gamaklub.ggwp.cc`
            - Automatically added to your multiplayer list!
            
            ---
            
            ### 📝 macOS Installation Instructions
            
            1. Download and open the DMG file
            2. Drag **GamaLauncher** to the **Applications** folder
            3. **Important:** Right-click the app and select **"Open"**
            4. Click **"Open"** again when warned
            5. The app is now trusted and will open normally!
            
            **Alternative (Terminal):**
            ```
            xattr -cr /Applications/GamaLauncher.app
            ```
            
            ---
            
            ### 🐛 Known Issues
            - macOS: Requires right-click → Open on first launch (unsigned app)
            - First launch may take 2-3 minutes (downloading Java + Minecraft)
            
            ### 💬 Support
            - Report issues on GitHub
            - Join our Discord: [Your Discord Link]
            
            ---
            
            **Full Changelog:** https://github.com/wckdg/gama-launcher/commits/v${{ steps.version.outputs.VERSION }}
          files: release/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
